datasets:
  cmtgen_mix_caption: # name of the dataset builder

    data_type: images # [images|videos|features]
    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          url: ""
          storage: ./mixed_coco_vg_sbu_mbcg_en_data.json
images:
        storage: /image/save/path
